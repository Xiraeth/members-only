doctype html
html
  head
    title= title
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    link(rel='stylesheet', href='/stylesheets/style.css')

  body.m-0.p-0
    nav.position-top.p-2#navbar
      ul.nav.justify-content-between.fs-4
        li.nav-item
          a.nav-link.active.mx-md-0.mx-auto(href='/') Home
        div.d-flex
          if !currentUser
            li.nav-item
              a.nav-link(href='/signup') Sign up
            li.nav-item
              a.nav-link(href='/login') Log in
          if currentUser
            if currentUser.isMember
              li.nav-item
                a.nav-link(href='' style="color: var(--already-member-color); pointer-events: none;") Already a member
            else 
              li.nav-item
                a.nav-link(href='/member') Become a member
            li.nav-item
              a.nav-link(href='/createMsg') Create message
            if !currentUser.isAdmin
                li.nav-item
                  a.nav-link#becomeAdmin(href='/admin') Become an admin
            else 
              li.nav-item 
                a.nav-link.text-decoration-underline(href="")(style="pointer-events: none; color: var(--already-admin-color);") ADMIN
            

    block content
